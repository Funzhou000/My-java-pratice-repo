package project.PuzzleGame.UI;

import javax.swing.JFrame;

public class RegistFrame extends JFrame {
    public RegistFrame() {
        setSize(488, 500);
        setTitle("Register v1.0");
        setAlwaysOnTop(true);
        // set location in middle of screen
        setLocationRelativeTo(null);
        setDefaultCloseOperation(3);
        setVisible(true);
    }
}
